name: inventory_backend
description: A shelf server for inventory management.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_cors_headers: ^0.1.5
  mysql1: ^0.20.0
  bcrypt: ^1.1.0
  dotenv: ^4.1.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
